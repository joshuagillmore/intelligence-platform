// Sentinel redesign: replaced by SentinelShell (Header + Rail + FooterBar) in layout.tsx.
// Kept as a no-op so legacy page imports continue to compile during the page-by-page redesign.
export default function Sidebar() {
  return null;
}
