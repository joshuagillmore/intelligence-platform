/**
 * Product branding — one source of truth so the app name and version never
 * drift across the shell, login, sidebar, and mobile header.
 */

// Canonical product name.
export const APP_NAME = 'SENTINEL';

// Single version string, shown wherever a build/version tag appears.
export const APP_VERSION = 'v2.4-alpha';

// Short descriptive tagline for spots with room for one.
export const APP_TAGLINE = 'Intelligence-analyst workbench';

// Browser tab / SEO title — product name plus a descriptor.
export const APP_TITLE = 'SENTINEL — Intelligence Platform';
