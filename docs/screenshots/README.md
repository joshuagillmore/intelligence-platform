# Screenshots

The root `README.md` references the images below. Capture each from a running
instance with representative (non-sensitive) data and drop the PNG here with the
exact filename — the README will then render it with no further edits.

Until the files exist, those `![...]` references render as broken-image icons on
GitHub, so **add these before making the repo public.**

| Filename | View | What to show |
|----------|------|--------------|
| `network-graph.png` | `/network` | d3 force graph with an entity selected — the ego-network highlight + right-hand entity/enrichment panel (the hero shot) |
| `geo-aoi.png` | `/geo` | Satellite basemap with connection lines and an area-of-interest box drawn; a marker popup showing MGRS/geo-confidence |
| `cyber-enrichment.png` | `/cyber` | The EnrichmentPanel for an investigated observable (IP/domain) with provider results |
| `products-intsum.png` | `/products` | A generated INTSUM (or Threat Assessment) with its citations / evidence chain |
| `topic-mindmap.png` | Topic mind-map | The radial mind-map expanded, ideally with a cross-reference link between topics lit up |
| `collection-plan.png` | `/collection-plans` | An objective → planned sources → run-status view |

A 10-second GIF of the collect → extract → graph loop (saved as `demo.gif` and
linked from the README hero) is worth more than any single still, if you can grab one.
